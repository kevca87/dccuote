# DCCuote BackEnd

Minimal aplication to manage a daily quote service backend.

## Run

To run the backend you first need to install the necesarry dependencies

```
pip install -r requirements
```

The you can execute the following command to run it on localhost

```
python -m flask run
```

## Doc

You can check the endpoints doc in

[http://127.0.0.1:5000/apidocs](http://127.0.0.1:5000/apidocs)